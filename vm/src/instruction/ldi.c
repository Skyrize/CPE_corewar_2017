/*
** EPITECH PROJECT, 2018
** null
** File description:
** null
*/

/*
int read_t_dir_ldi(byte *tab, pc_t *pc, champ_t *champ)
{

}

int read_t_ind_ldi(byte *tab, pc_t *pc, champ_t *champ)
{

}

void operate_ldi()
{
	int *parameters = detect_parameters(my_getnbr(tab + pc->idx + 1));

	if (parameters[0] == DIR_SIZE)
		return (read_t_dir_ldi(tab, pc, champ));
	else if (parameters[0] == IND_SIZE)
		return (read_t_ind_ldi(tab, pc, champ));
	if (parameters[0] == T_REG)

	return (0);
}
*/